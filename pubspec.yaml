name: aligned_tooltip
description: A multi directional tooltip used to display any content
version: 0.0.1
homepage: https://github.com/KevinVan720/aligned_tooltip

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  golden_toolkit: ^0.13.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
